# Cat vs Dog Classifier

This is a deep learning web app built using TensorFlow and Streamlit.

## Features
- Upload an image
- Predict whether it is a cat or dog
- CNN model trained on image dataset

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
